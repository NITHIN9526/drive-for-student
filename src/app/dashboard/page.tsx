"use client";

import { Bell, Bookmark, ChevronDown, FileText, Filter, Home, LayoutGrid, LogOut, Menu, Plus, Search, Share2, Sparkles, UserRound, X } from "lucide-react";
import Link from "next/link";
import { ThemeToggle } from "@/components/theme-toggle";
import { AccountSettings } from "@/components/account-settings";
import { useEffect, useState } from "react";
import { createClient } from "@/lib/supabase/client";

const materials = [
  { title: "Operating Systems — Unit 1 to 5", subject: "Operating Systems", meta: "CSE · 4th sem · PDF", type: "pdf", color: "rose", author: "Priya Shah", time: "2h ago", saves: 24 },
  { title: "Graphs in 15 minutes", subject: "Algorithms", meta: "CSE · 4th sem · Video", type: "video", color: "sky", author: "Aditya Menon", time: "5h ago", saves: 18 },
  { title: "DBMS Interview Prep Guide", subject: "Database Systems", meta: "CSE · 4th sem · Note", type: "note", color: "amber", author: "Neha Kulkarni", time: "Yesterday", saves: 31 },
  { title: "Computer Networks — PYQs", subject: "Computer Networks", meta: "CSE · 4th sem · PDF", type: "pdf", color: "violet", author: "Rahul Verma", time: "Yesterday", saves: 12 },
];

function UploadModal({ close }: { close: () => void }) {
  const [type, setType] = useState("PDF");
  const [isPublic, setIsPublic] = useState(true);
  const [title, setTitle] = useState("");
  const [subject, setSubject] = useState("");
  const [message, setMessage] = useState("");
  async function publish() {
    const supabase = createClient();
    const { data: { user } } = await supabase.auth.getUser();
    if (!user || !title.trim()) { setMessage("Add a title before publishing."); return; }
    const { error } = await supabase.from("materials").insert({ uploader_id: user.id, type: type === "YouTube Link" ? "youtube" : type === "Text Note" ? "note" : "pdf", title: title.trim(), subject: subject.trim(), is_public: isPublic });
    if (error) {
      setMessage(error.message.includes("is_public") ? "Database setup is incomplete. Run supabase/migrations/20250906_material_visibility.sql in Supabase SQL Editor, then try again." : error.message);
    } else close();
  }
  return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4 backdrop-blur-sm"><div className="w-full max-w-xl rounded-[2rem] bg-white p-7 shadow-2xl"><div className="mb-7 flex items-start justify-between"><div><div className="mb-3 inline-flex rounded-xl bg-indigo-50 p-3 text-indigo-600"><Sparkles size={19} /></div><h2 className="font-display text-2xl font-bold">Add to the shelf</h2><p className="mt-1 text-sm text-slate-500">Share something that helped you learn.</p></div><button onClick={close} className="rounded-full p-2 text-slate-400 hover:bg-slate-100" aria-label="Close"><X size={19} /></button></div><div className="mb-5 grid grid-cols-3 gap-2">{["PDF", "YouTube Link", "Text Note"].map((item) => <button key={item} onClick={() => setType(item)} className={`rounded-xl border p-3 text-sm font-semibold ${type === item ? "border-indigo-600 bg-indigo-50 text-indigo-700" : "border-slate-200 text-slate-500"}`}>{item}</button>)}</div><label className="mb-1 block text-sm font-semibold">Title</label><input value={title} onChange={(e) => setTitle(e.target.value)} placeholder="e.g. Unit 2 revision notes" className="mb-4 h-12 w-full rounded-xl border border-slate-200 px-4 outline-none focus:border-indigo-500" /><div className="grid gap-4 sm:grid-cols-2"><div><label className="mb-1 block text-sm font-semibold">Subject</label><input value={subject} onChange={(e) => setSubject(e.target.value)} placeholder="Operating Systems" className="h-12 w-full rounded-xl border border-slate-200 px-4 outline-none focus:border-indigo-500" /></div><div><label className="mb-1 block text-sm font-semibold">Semester</label><select className="h-12 w-full rounded-xl border border-slate-200 bg-white px-4 text-slate-500"><option>4th semester</option><option>3rd semester</option><option>5th semester</option></select></div></div><div className="mt-4 flex items-center justify-between rounded-xl bg-slate-50 p-4"><div><p className="text-sm font-semibold">Visibility</p><p className="mt-1 text-xs text-slate-500">{isPublic ? "Anyone in your study community can find this." : "Only you can view this material."}</p></div><button onClick={() => setIsPublic((value) => !value)} className={`rounded-full px-3 py-2 text-xs font-bold ${isPublic ? "bg-emerald-100 text-emerald-700" : "bg-slate-200 text-slate-600"}`}>{isPublic ? "Public" : "Private"}</button></div>{type === "PDF" ? <div className="mt-4 rounded-2xl border-2 border-dashed border-slate-200 p-6 text-center"><FileText className="mx-auto text-slate-400" /><p className="mt-2 text-sm font-semibold">Drop your PDF here</p><p className="mt-1 text-xs text-slate-400">Maximum file size 20 MB</p></div> : type === "YouTube Link" ? <input placeholder="https://youtube.com/watch?v=..." className="mt-4 h-12 w-full rounded-xl border border-slate-200 px-4 outline-none focus:border-indigo-500" /> : <textarea placeholder="Write your note..." className="mt-4 h-28 w-full resize-none rounded-xl border border-slate-200 p-4 outline-none focus:border-indigo-500" /> }{message && <p className="mt-3 text-sm text-rose-600">{message}</p>}<button onClick={publish} className="mt-6 flex h-12 w-full items-center justify-center rounded-xl bg-indigo-600 font-semibold text-white hover:bg-indigo-700">Publish material</button></div></div>;
}

export default function Dashboard() {
  const [uploadOpen, setUploadOpen] = useState(false);
  const [mobileNav, setMobileNav] = useState(false);
  const [settingsOpen, setSettingsOpen] = useState(false);
  const [activeView, setActiveView] = useState<"home" | "saved" | "uploads" | "profile">("home");
  const [profile, setProfile] = useState({ username: "student", full_name: "Student", college: "", branch: "", semester: null as number | null, avatar_url: null as string | null });
  const [userEmail, setUserEmail] = useState("");
  useEffect(() => {
    const supabase = createClient();
    async function loadProfile() {
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) return;
      setUserEmail(user.email ?? "");
      const metadata = user.user_metadata;
      const fallbackName = metadata.full_name ?? metadata.name ?? user.email?.split("@")[0] ?? "Student";
      const fallbackUsername = `${fallbackName.toLowerCase().replace(/[^a-z0-9]/g, "").slice(0, 24) || "student"}_${user.id.replace(/-/g, "").slice(-6)}`;
      const { data } = await supabase.from("profiles").select("username, full_name, college, branch, semester, avatar_url").eq("id", user.id).maybeSingle();
      if (data) setProfile(data);
      else {
        const { data: created } = await supabase.from("profiles").upsert({ id: user.id, username: fallbackUsername, full_name: fallbackName, avatar_url: metadata.avatar_url ?? null }).select().single();
        if (created) setProfile(created);
      }
    }
    loadProfile();
  }, []);
  const viewTitle = activeView === "saved" ? "Saved materials" : activeView === "uploads" ? "My uploads" : activeView === "profile" ? "My profile" : "Your study shelf";
  const viewDescription = activeView === "saved" ? "Everything you bookmarked for later." : activeView === "uploads" ? "Resources you have shared with your community." : activeView === "profile" ? "Manage your public profile and account details." : "A little progress is still progress. Let's make some today.";
  const viewMaterials = activeView === "saved" ? materials.slice(0, 2) : activeView === "uploads" ? [] : materials;
  return <main className="min-h-screen bg-[#f7f8fc] text-slate-900"><aside className={`fixed inset-y-0 left-0 z-40 w-64 border-r border-slate-100 bg-white p-5 transition-transform lg:translate-x-0 ${mobileNav ? "translate-x-0" : "-translate-x-full"}`}><div className="mb-12 flex items-center gap-2.5 px-2 font-display text-xl font-bold"> <span className="grid h-9 w-9 place-items-center rounded-xl bg-indigo-600 text-white"><LayoutGrid size={18} /></span> drive<span className="text-indigo-600">.</span><button onClick={() => setMobileNav(false)} className="ml-auto lg:hidden"><X size={18} /></button></div><p className="px-2 text-[10px] font-bold uppercase tracking-[.18em] text-slate-400">Workspace</p>  <nav className="mt-3 space-y-1">{[[Home, "Home", "home"], [Bookmark, "Saved materials", "saved"], [FileText, "My uploads", "uploads"], [UserRound, "My profile", "profile"]].map(([Icon, label, view]) => <button onClick={() => view === "profile" ? setSettingsOpen(true) : setActiveView(view as "home" | "saved" | "uploads")} key={label as string} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-semibold ${activeView === view ? "bg-indigo-50 text-indigo-700" : "text-slate-500 hover:bg-slate-50"}`}><Icon size={18} />{label as string}</button>)}</nav><p className="mt-10 px-2 text-[10px] font-bold uppercase tracking-[.18em] text-slate-400">Your space</p><div className="mt-3 rounded-2xl bg-slate-900 p-4 text-white"><p className="text-sm font-semibold">Share knowledge</p><p className="mt-1 text-xs leading-5 text-slate-400">Help your course-mates find their next breakthrough.</p><button onClick={() => setUploadOpen(true)} className="mt-4 flex w-full items-center justify-center gap-2 rounded-lg bg-indigo-500 py-2 text-xs font-bold"><Plus size={15} /> Upload</button></div><Link href="/" className="absolute bottom-6 left-8 flex items-center gap-3 text-sm font-semibold text-slate-400 hover:text-slate-700"><LogOut size={17} /> Log out</Link></aside><div className="lg:pl-64"><header className="flex h-20 items-center justify-between border-b border-slate-100 bg-white px-5 sm:px-8"><button onClick={() => setMobileNav(true)} className="lg:hidden"><Menu /></button><div className="hidden items-center gap-3 text-sm text-slate-400 sm:flex"><span>Workspace</span><span>/</span><span className="font-semibold text-slate-700">Home</span></div>  <div className="flex items-center gap-3"><ThemeToggle /><button className="grid h-10 w-10 place-items-center rounded-xl text-slate-400 hover:bg-slate-50"><Bell size={19} /></button>  <button onClick={() => setSettingsOpen(true)} className="flex items-center gap-2 border-l border-slate-100 pl-3"><div className="grid h-9 w-9 rounded-full bg-amber-100 place-items-center text-xs font-bold text-amber-700">{profile.full_name.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase()}</div><span className="hidden max-w-32 truncate text-sm font-semibold sm:block">{profile.full_name}</span><ChevronDown size={15} className="text-slate-400" /></button></div></header><div className="mx-auto max-w-6xl px-5 py-8 sm:px-8"><section className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-indigo-600">Tuesday, 12 November</p>  <h1 className="mt-2 font-display text-3xl font-bold tracking-tight sm:text-4xl">{viewTitle}<span className="text-indigo-600">.</span></h1><p className="mt-2 text-slate-500">{viewDescription}</p></div><button onClick={() => setUploadOpen(true)} className="flex h-12 items-center justify-center gap-2 rounded-xl bg-indigo-600 px-5 font-semibold text-white shadow-lg shadow-indigo-100 hover:bg-indigo-700"><Plus size={18} /> Upload material</button></section><div className="mt-8 grid gap-4 rounded-2xl bg-indigo-600 p-5 text-white sm:grid-cols-[1fr_auto] sm:items-center sm:p-7"><div><p className="text-sm font-semibold text-indigo-200">Quick find</p><h2 className="mt-1 font-display text-xl font-bold">What are you looking for?</h2></div><div className="relative sm:w-80"><Search className="absolute left-3 top-3.5 text-slate-400" size={18} /><input placeholder="Search materials, subjects..." className="h-11 w-full rounded-xl bg-white pl-10 pr-3 text-sm text-slate-700 outline-none placeholder:text-slate-400" /></div></div><div className="mt-10 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h2 className="font-display text-xl font-bold">Made for your semester</h2><p className="mt-1 text-sm text-slate-500">Fresh resources from your study community.</p></div><div className="flex gap-2"><button className="flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600"><Filter size={14} /> Filters</button><button className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600">Recent <ChevronDown className="ml-1 inline" size={13} /></button></div></div>  <div className="mt-5 grid gap-4 md:grid-cols-2">{viewMaterials.length === 0 && <div className="rounded-2xl border border-dashed border-slate-200 bg-white p-8 text-center md:col-span-2"><p className="font-display font-bold">Nothing here yet.</p><p className="mt-1 text-sm text-slate-500">Upload your first resource to start building your study shelf.</p><button onClick={() => setUploadOpen(true)} className="mt-4 rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Upload material</button></div>}  {viewMaterials.map((m) => <a href="#" key={m.title} className="group rounded-2xl border border-slate-100 bg-white p-5 transition hover:-translate-y-0.5 hover:shadow-xl hover:shadow-slate-100"><div className="flex items-start justify-between"><div className={`icon-box bg-${m.color}-100 text-${m.color}-600`}><FileText size={19} /></div><button className="text-slate-300 hover:text-indigo-600" aria-label="Save material"><Bookmark size={19} /></button></div><h3 className="mt-5 font-display font-bold group-hover:text-indigo-600">{m.title}</h3><p className="mt-1 text-sm text-slate-400">{m.meta}</p><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4 text-xs text-slate-400"><span>By <strong className="text-slate-600">{m.author}</strong> · {m.time}</span><span className="flex items-center gap-1"><Bookmark size={13} /> {m.saves}</span></div></a>)}</div><div className="mt-10 flex items-center justify-between"><h2 className="font-display text-xl font-bold">Study circles</h2><a href="#" className="text-sm font-semibold text-indigo-600">View all <Share2 className="ml-1 inline" size={14} /></a></div><div className="mt-4 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-amber-50 p-5"><p className="text-xs font-bold uppercase tracking-wider text-amber-700">Most active</p><p className="mt-3 font-display font-bold text-amber-950">Computer Science</p><p className="mt-1 text-sm text-amber-800/70">428 resources shared</p></div><div className="rounded-2xl bg-emerald-50 p-5"><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">New circle</p><p className="mt-3 font-display font-bold text-emerald-950">Design &amp; HCI</p><p className="mt-1 text-sm text-emerald-800/70">63 students learning</p></div><div className="rounded-2xl bg-violet-50 p-5"><p className="text-xs font-bold uppercase tracking-wider text-violet-700">Your branch</p><p className="mt-3 font-display font-bold text-violet-950">4th Semester CSE</p><p className="mt-1 text-sm text-violet-800/70">92 new this week</p></div></div>  </div></div>{uploadOpen && <UploadModal close={() => setUploadOpen(false)} />}{settingsOpen && <AccountSettings profile={profile} userEmail={userEmail} close={() => setSettingsOpen(false)}   onSaved={(nextProfile) => { setProfile(nextProfile); setSettingsOpen(false); }} onLogout={() => setSettingsOpen(false)} />}</main>;
}
